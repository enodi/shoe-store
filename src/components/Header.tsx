import { Box, Heading } from "@chakra-ui/react";

const Header = () => (
  <Box sx={{ bg: "gray.400" }}>
    <Heading sx={{ textAlign: "center", p: 3, color: "#FFF" }}>
      Aldo Store
    </Heading>
  </Box>
);

export default Header;
